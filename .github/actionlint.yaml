self-hosted-runner:
  # ghost-runners is the ARC (actions-runner-controller) self-hosted scale set
  # documented in AGENTS.md "ARC Runners" — it lives on the ghost k3s cluster
  # and is the only bridge with LAN access to Argo/k8s for this repo's
  # self-hosted cluster snapshot job.
  labels:
    - ghost-runners
