apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: bluefin-service-catalog-
  namespace: argo
spec:
  serviceAccountName: argo
  workflowTemplateRef:
    name: bluefin-service-catalog-pipeline
  arguments:
    parameters:
      - name: lane
        value: "media"
      - name: branch
        value: "main"
