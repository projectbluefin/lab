apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: homelab-restore-
  namespace: argo
spec:
  serviceAccountName: argo
  workflowTemplateRef:
    name: homelab-restore-drill
  arguments:
    parameters:
      - name: branch
        value: "main"
