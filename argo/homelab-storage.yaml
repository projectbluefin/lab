apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: homelab-storage-
  namespace: argo
spec:
  serviceAccountName: argo
  workflowTemplateRef:
    name: homelab-storage
  arguments:
    parameters:
      - name: branch
        value: "main"
