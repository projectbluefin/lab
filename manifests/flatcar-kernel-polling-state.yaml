# Tracks the last-seen kernel.org stable version.
# flatcar-kernel-poller patches kernel-stable when a new version is found.
apiVersion: v1
kind: ConfigMap
metadata:
  name: flatcar-kernel-polling-state
  namespace: argo
  labels:
    app.kubernetes.io/part-of: testing-lab
