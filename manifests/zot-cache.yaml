# Zot pull-through cache — single instance proxying all 8 upstream registries
# Zot sync destination prefix mapping stores each registry under a separate subpath:
#   ghcr.io                    → /ghcr   (pull via http://192.168.1.102:30501/ghcr/...)
#   docker.io                  → /docker (pull via http://192.168.1.102:30501/docker/...)
#   quay.io                    → /quay   (pull via http://192.168.1.102:30501/quay/...)
#   registry.fedoraproject.org → /fedora (pull via http://192.168.1.102:30501/fedora/...)
#   registry.k8s.io            → /k8s    (pull via http://192.168.1.102:30501/k8s/...)
#   cgr.dev                    → /cgr    (pull via http://192.168.1.102:30501/cgr/...)
#   public.ecr.aws             → /ecr    (pull via http://192.168.1.102:30501/ecr/...)
#   lscr.io                    → /lscr   (pull via http://192.168.1.102:30501/lscr/...)
#
# Sync content filters use UPSTREAM repository patterns; `destination` supplies
# the local path segment. So quay.io/argoproj/argocd is matched by
# prefix "argoproj/*" + destination "/quay" and served as /quay/argoproj/argocd.
# Do NOT write the destination segment into the prefix (e.g. "quay/argoproj/*") —
# it will never match upstream and on-demand sync silently 404s. Registries with
# no prefix list must still declare a destination, or their images land at the
# wrong path. Filters exist so unsupported pulls fail without probing every
# upstream repository.
# containerd on each node routes pulls through this via
# manifests/registry-mirror-config.yaml (hosts.toml DaemonSet, no k3s restart needed)
#
# Zot acts as a pull-through cache. Containerd sends requests to the mirror
# namespace and Zot fetches from the upstream registry on-demand on the first
# pull, then serves the cached copy for later pulls.
#
# Storage: hostPath on ghost at /var/mnt/ghost-data/zot-cache/
# ArgoCD: lab-infra syncs this
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: zot-cache-config
  namespace: local-registry
  labels:
    app.kubernetes.io/part-of: lab
data:
  config.json: |
    {
      "distSpecVersion": "1.1.0",
      "storage": {
        "rootDirectory": "/var/lib/registry",
        "gc": true
      },
      "http": {
        "address": "0.0.0.0",
        "port": "5000"
      },
      "log": { "level": "info" },
      "extensions": {
        "metrics": {
          "enable": true,
          "prometheus": {
            "path": "/metrics"
          }
        },
        "sync": {
          "enable": true,
          "registries": [
            {
              "urls": ["https://ghcr.io"],
              "onDemand": true,
              "tlsVerify": true,
              "maxRetries": 1,
              "retryDelay": "2m",
              "content": [
                { "prefix": "actions/*", "destination": "/ghcr" },
                { "prefix": "buildbarn/*", "destination": "/ghcr" },
                { "prefix": "frostyard/*", "destination": "/ghcr" },
                { "prefix": "ggml-org/*", "destination": "/ghcr" },
                { "prefix": "jrottenberg/*", "destination": "/ghcr" },
                { "prefix": "kubestellar/**", "destination": "/ghcr" },
                { "prefix": "oras-project/*", "destination": "/ghcr" },
                { "prefix": "project-zot/*", "destination": "/ghcr" },
                { "prefix": "projectbluefin/*", "destination": "/ghcr" },
                { "prefix": "ublue-os/*", "destination": "/ghcr" }
              ]
            },
            {
              "urls": ["https://registry-1.docker.io"],
              "onDemand": true,
              "tlsVerify": true,
              "maxRetries": 1,
              "retryDelay": "2m",
              "content": [
                { "prefix": "bitnami/*", "destination": "/docker" },
                { "prefix": "curlimages/*", "destination": "/docker" },
                { "prefix": "jrottenberg/*", "destination": "/docker" },
                { "prefix": "library/*", "destination": "/docker" },
                { "prefix": "linuxserver/*", "destination": "/docker" },
                { "prefix": "rancher/*", "destination": "/docker" },
                { "prefix": "rocm/*", "destination": "/docker" },
                { "prefix": "vllm/*", "destination": "/docker" }
              ]
            },
            {
              "urls": ["https://quay.io"],
              "onDemand": true,
              "tlsVerify": true,
              "maxRetries": 1,
              "retryDelay": "2m",
              "content": [
                { "prefix": "argoproj/*", "destination": "/quay" },
                { "prefix": "brancz/*", "destination": "/quay" },
                { "prefix": "buildah/*", "destination": "/quay" },
                { "prefix": "fedora/*", "destination": "/quay" },
                { "prefix": "fedora-ostree-desktops/*", "destination": "/quay" },
                { "prefix": "kubestellar/*", "destination": "/quay" },
                { "prefix": "kubevirt/*", "destination": "/quay" },
                { "prefix": "open-cluster-management/*", "destination": "/quay" },
                { "prefix": "podman/*", "destination": "/quay" },
                { "prefix": "prometheus/*", "destination": "/quay" },
                { "prefix": "skopeo/*", "destination": "/quay" }
              ]
            },
            {
              "urls": ["https://registry.fedoraproject.org"],
              "onDemand": true,
              "tlsVerify": true,
              "maxRetries": 1,
              "retryDelay": "2m",
              "content": [{ "prefix": "fedora*", "destination": "/fedora" }]
            },
            {
              "urls": ["https://registry.k8s.io"],
              "onDemand": true,
              "tlsVerify": true,
              "maxRetries": 1,
              "retryDelay": "2m",
              "content": [
                { "prefix": "kube-apiserver", "destination": "/k8s" },
                { "prefix": "kube-controller-manager", "destination": "/k8s" },
                { "prefix": "metrics-server/*", "destination": "/k8s" },
                { "prefix": "nfd/*", "destination": "/k8s" },
                { "prefix": "pause", "destination": "/k8s" }
              ]
            },
            {
              "urls": ["https://cgr.dev"],
              "onDemand": true,
              "tlsVerify": true,
              "maxRetries": 1,
              "retryDelay": "2m",
              "content": [{ "prefix": "chainguard/*", "destination": "/cgr" }]
            },
            {
              "urls": ["https://public.ecr.aws"],
              "onDemand": true,
              "tlsVerify": true,
              "maxRetries": 1,
              "retryDelay": "2m",
              "content": [
                { "prefix": "docker/library/*", "destination": "/ecr" }
              ]
            },
            {
              "urls": ["https://lscr.io"],
              "onDemand": true,
              "tlsVerify": true,
              "maxRetries": 1,
              "retryDelay": "2m",
              "content": [{ "prefix": "linuxserver/*", "destination": "/lscr" }]
            }
          ]
        }
      }
    }
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: zot-cache
  namespace: local-registry
  labels:
    app.kubernetes.io/part-of: lab
    app.kubernetes.io/component: registry-cache
spec:
  selector:
    matchLabels:
      app: zot-cache
  template:
    metadata:
      labels:
        app: zot-cache
        app.kubernetes.io/part-of: lab
      annotations:
        lab.projectbluefin.io/config-version: sync-policy-v7
    spec:
      automountServiceAccountToken: false
      priorityClassName: system-cluster-critical
      # Capability-based placement: only nodes carrying the zot-cache data
      # disk run the cache (label ghost once:
      #   kubectl label node ghost lab.projectbluefin.io/zot-cache-data=true).
      # Without this the DaemonSet lands on every node and fails on hosts
      # that lack /var/mnt/ghost-data (exo-0 sat in CreateContainerError for
      # 14d and wedged every lab-infra sync at "waiting for healthy state").
      nodeSelector:
        lab.projectbluefin.io/zot-cache-data: "true"
      containers:
        - name: zot
          image: ghcr.io/project-zot/zot-linux-amd64:v2.1.1
          args: ["serve", "/etc/zot/config.json"]
          ports:
            - containerPort: 5000
          volumeMounts:
            - name: config
              mountPath: /etc/zot/config.json
              subPath: config.json
            - name: data
              mountPath: /var/lib/registry
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "2", memory: "4Gi" }
          readinessProbe:
            httpGet:
              path: /v2/
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: config
          configMap:
            name: zot-cache-config
        - name: data
          hostPath:
            # Moved back onto the XFS ghost-data disk 2026-07-03: the prior
            # node-local btrfs path (/var/mnt/framework-cache, fb9866d0) grew
            # to 286G and pushed the root disk to 88% full. Root disk stability
            # takes priority over the marginal local-btrfs throughput gain.
            path: /var/mnt/ghost-data/zot-cache
            type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: zot-cache
  namespace: local-registry
  labels:
    app.kubernetes.io/part-of: lab
spec:
  selector:
    app: zot-cache
  ports:
    - port: 5000
      targetPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: zot-cache-nodeport
  namespace: local-registry
  labels:
    app.kubernetes.io/part-of: lab
spec:
  type: NodePort
  externalTrafficPolicy: Local
  selector:
    app: zot-cache
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30501
